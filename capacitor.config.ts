import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'M-expense-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
